@model IEnumerable<Eregister.TeacherSubject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subject.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subject.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeacherSubjectID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeacherSubjectID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeacherSubjectID })
        </td>
    </tr>
}

</table>
